#1866
#include <iostream>
#include <cstring>
#include <fstream>

using namespace std;
ifstream fin ("prosir.in");
ofstream fout("prosir.out");
bool literea(char ch)
{
        return ch>='a' && ch<='z';
}
int main()
{
   char s[205];
   fin.getline(s, 203);
   for(int i=0; s[i]!='.'; i++)
   {
       if((s[i+1]==' ' || s[i+1]=='.') && literea(s[i]))
         s[i]='5';
   }

 fout<<s;
    return 0;
}

=======================================================================

#1456
#include <iostream>
#include <cstring>
#include <fstream>

using namespace std;

int main()
{
    char s[101];
    cin>>s;
    bool cons=false, voc=false;
    for(int i=0; i<strlen(s); i++)
        if(!strchr("aeiou", s[i]))
            cons=true;
          else
          {
              if(strchr("aeou", s[i]))
                voc=true;
          }
    if(!voc && cons)
        cout<<"DA";
      else
      cout<<"NU";
    return 0;
}

==============================

#2828
#include <iostream>
#include <cstring>
#include <fstream>

using namespace std;
bool litermare(char ch)
{
    return ch>='A' && ch<='Z';
}

int main()
{
    char s[101], id[50];
    cin.getline(s, 101);
    for(int i=0; i<strlen(s); i++)
    {
        if(litermare(s[i]) && (i==0 || s[i-1]==' '))
          cout<<s[i];
    }
    return 0;
}

==========================================

#890
#include <iostream>
#include <cstring>

using namespace std;
int f[200];
int main()
{
    char s[256];
    cin.getline(s, 256);
    for(int i=0 ;i<strlen(s); i++)
    {
        if(strchr("AEIOUaeiou", s[i]))
        {
            if(s[i]>='a' && s[i]<='z')
                s[i]-=32;
            f[(int)s[i]]++;
        }
    }
    int maxi=0;
    char rez;
    for(int i='A'; i<='Z'; i++)
        if(strchr("AEIOU", i) && f[i]>maxi)
        {
           maxi=f[i];
           rez=i;
        }
    cout<<rez;
    return 0;
}

================================================

#13 
#include <iostream>
#include <cstring>
using namespace std;

int main(){
    char s[15],t[15];
    cin >> s;
    strcpy(t,s);
    for(int i=strlen(t); i>0;--i){
        t[i] = 0;
        cout << t << endl;
    }
    for(int i=0;s[i]; ++i)
        cout << s+i <<endl;
    return 0;
}
================================================

#84
#include <bits/stdc++.h>
using namespace std;
int main()
{
    char s[11];
    cin>>s;
    int iv=-1;
    int ic=-1;
    for(int i=0; s[i]; i++)
    {
        if(strchr("AEIOUaeiou", s[i]) && iv==-1)
           iv=i;
        if(!strchr("AEIOUaeiou", s[i]))
           ic=i;
    }
    if(ic==-1 || iv==-1)
        cout<<"IMPOSIBIL";
     else
     {
         swap(s[iv], s[ic]);
         cout<<s;
     }
    return 0;
}

==========================================================

#85
#include <bits/stdc++.h>

using namespace std;

int main()
{
    char s[101];
    cin.getline(s, 101);
    int n=strlen(s);
    int aux=strlen(s);
    for(int i=0; s[i]; i++)
    {
        if(strchr("aeiou", s[i]))
        {
            for(int j=n+1; j>i; j--)
                s[j]=s[j-1];
            s[i+1]='*';
            n++;
            i++;
        }

    }
    if(n==aux)
        cout<<"FARA VOCALE";
      else
       cout<<s;
    return 0;
}

==================================================

#2635
#include <bits/stdc++.h>

using namespace std;
ifstream fin ("capslock.in");
ofstream fout ("capslock.out");
  char s[256];
int main()
{
  
    fin.getline(s, 256);
    for(int i=0; i<strlen(s); i++)
        if(s[i]=='#')
        {
            int j=i+1;
            while(s[j] && s[j]!='#')
            {
                s[j]=toupper(s[j]);
                j++;
            }
            i=j;
        }
    for(int i=0; i<strlen(s); i++)
        if(s[i]!='#')
         fout<<s[i];

    return 0;
}

=======================================================

#88
#include <bits/stdc++.h>

using namespace std;
ifstream fin ("palindrom.in");
ofstream fout ("palindrom.out");
bool palindrom (char s[])
{
    for(int i=0, j=strlen(s)-1;  i<j; i++, j--)
        if(s[i]!=s[j])
          return 0;
    return 1;
}
int main()
{
    int n;
     char s[21];
    fin>>n;
    for(int i=1; i<=n; i++)
    {
       
        fin>>s;
        fout<<palindrom(s)<<endl;
    }
 
    return 0;
}

=================================================

#90
#include <bits/stdc++.h>

using namespace std;
ifstream fin ("vocmax.in");
ofstream fout ("vocmax.out");

int main()
{
    char a[101][251];
    int n;
    fin>>n;
    int maxi=0, indice=0;
    for(int i=0; i<=n; i++)
    {
        fin.getline(a[i], 251);
        int cnt=0;
        for(int j=0; a[i][j]!='\0';  j++)
            if(strchr("aeiou", a[i][j]))
                cnt++;
        if(cnt>maxi)
        {
            maxi=cnt;
            indice=i;
        }

    }
    for(int j=0; a[indice][j]!='\0'; j++)
        fout<<a[indice][j];
    return 0;
}

==============================================================

#973
#include <bits/stdc++.h>

using namespace std;


int main()
{
  char s[256];
  cin.getline(s, 256);
  char sep[]=" ";
  char *p=strtok(s, sep);
  while(p)
  {
      bool ok=true;
      for(int i=0; p[i]; i++)
        if(strchr("aeiou", p[i])==NULL)
           ok=false;
      if(ok==true)
        cout<<p<<'\n';
      p=strtok(NULL, sep);
  }
    return 0;
}

====================================================

#975
#include <bits/stdc++.h>

using namespace std;

bool palindrom(char s[])
{
    for(int i=0, j=strlen(s)-1; i<j; i++, j--)
        if(s[i]!=s[j])
          return false;
    return true;
}

int main()
{
  char s[256];
  cin.getline(s, 256);
  char sep[]=" ,.";
  char m[255][11];
  int ind=0;
  char *p=strtok(s, sep);
  while(p)
  {

      strcpy(m[++ind], p);
      p=strtok(NULL, sep);
  }
  for(int i=1; i<ind; i++)
    for(int j=i+1; j<=ind; j++)
      if(strcmp(m[i], m[j])>0)
        {
            char aux[11];
            strcpy(aux, m[i]);
            strcpy(m[i],m[j]);
            strcpy(m[j], aux);
        }
  int ok=0;
  for(int i=1; i<=ind; i++)
  {
      if(palindrom(m[i]))
      {
          ok=1;
          cout<<m[i];
          break;
      }
  }
  if(!ok)
    cout<<"IMPOSIBIL";
    return 0;
}
==============================================

BAC 2022 AUGUST
#include <iostream>
#include <cstring>

using namespace std;

char vocala (char ch)
{
    int i=(int)ch;
    int cnt=0;
    while(cnt==0)
    {
        if(strchr("aeiou", (char)i))
            cnt=i;
        i--;
    }
    i++;
    return (char)i;
}

int main()
{
    char s[51];
    cin>>s;
    for(int i=0; i<strlen(s); i++)
    {
        if(!strchr("aeiou", s[i]))
         cout<<vocala(s[i]);
        else
         cout<<s[i];
    }
    return 0;
}

================================================

BAC 2022 IUNIE
#include <iostream>
#include <cstring>

using namespace std;

int main()
{
   char s[51], id[51];
   cin.getline(s, 51);
   char *p=strtok(s, " ");
   int i=1;
   while(i==1)
   {
       p=strtok(NULL, " ");
       strcpy(id, p);
       i++;
   }
   strcat(id, "2022");
   cout<<id;
    return 0;
}

===================================================

BAC MARTIE 2022 nu imi ia si primul element
#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    char s[251], x[21];
    cin.getline(s, 251);
    cin.getline(x, 21);
    char sep[]=";";
    char m[251][21];
    int ind=0, cnt=0;
    char *p1=strtok(s, sep);
    while(p1)
    {
        strcpy(m[++ind], p1);
        p1=strtok(NULL, sep);
    }
    for(int i=0; i<=ind; i++)
    {
       char *p2=strtok(m[i], " ");
       if(i==0 && strcmp(m[i], x)==0)
       {
           p2=strtok(NULL, " ");
           cout<<p2<<" ";
           cnt++;
       }
       else
         if(strcmp(m[i]+1, x)==0 && i!=0)
       {
           p2=strtok(NULL, " ");
           cout<<p2<<" ";
           cnt++;
       }
    }
    if(cnt==0)
        cout<<"NU";
    return 0;
}

===============================================================

BAC AUGUST 2021
II. 2 ARMONIA
=================================================================

BAC IUNIE 2021 cv nu i bun
#include <iostream>
#include <cstring>

using namespace std;

int main()
{
   int n, k;
   char s[21][10];
   cin>>n>>k;
   for(int i=1; i<=n; i++)
   {
       cin.getline(s[i], 11);
       char *p=strtok(s[i], "aeiou");
       int cnt=0;
       while(p || cnt==0)
       {
           if((p+1)=='\0')
             cnt++;
           p=strtok(NULL, "aeiou");
       }
       if(cnt==1 && k!=0)
        {
            cout<<s[i]<<'\n';
            k--;
       }
   }
    return 0;
}

=====================================================

BAC MAI 2021
II. 3.  2021
        b!a!c!

=========================================================

BAC MARTIE 2021
