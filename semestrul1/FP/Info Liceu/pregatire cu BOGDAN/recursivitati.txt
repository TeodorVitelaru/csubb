#include <fstream>

using namespace std;

int n,x[10],used[10];
void write()
{
    for(int i=1;i<=n;i++)
        fout<<x[i]<<" ";
    fout<<endl;
}
void f(int k)
{
    if(k==n+1)
        write();
    for(int i=1;i<=n;i++)
    {
        if(!used[i])
        { x[k]=i;
          used[i]=1;
          f(k+1);
          used[i]=0;}
    }
}

int main()
{
    fin>>n;
    f(1);
    return 0;
}
Pentru n=3 ???
--------
f(1): i=1, used[1]=1,x[1]=1,f(2) x
f(2): i=1 -
      i=2 used[2]=1,x[2]=2,f(3),x
f(3): i=1-
      i=2-
      i=3 used[3]=1,x[3]=3,f(4),x
f(4): afisam 1 2 3
<- in f(3) used[3]=0
<-- in f(2) used[2]=0
            i=3 used[3]=1,x[2]=3,f(3),x

123,132,213,231,...
///////////////////////
#include <bits/stdc++.h>
using namespace std;

int v[10], n,used[11],m, a, b;
void scrie() {
  for (int i = 1; i <= n; i++)
    cout << v[i] << " ";
  cout << "\n";
}

void f(int k) {
  if (k == n + 1) {
    scrie();
    return;
  }

	for (int i =a; i <= b; i++) 
	 if(!used[i-a])
	 { used[i-a]=1;   
	   v[k] = i;
           f(k + 1);
	   used[i-a]=0;
  }
}// 1<=a<=b<=10000000, b-a<=10
int main() {
  cin>>a>>b;
  n=b-a+1;
  f(1);
  
  return 0;
}
//a=5 b=7 Ce se afiseaza??
////////////////////////////////
567,576,
FRECVENTA LITERELOR DINTR-UN TEXT
int v[26]={0};
for(int i=0;s[i];i++)
 if(s[i]>='a' && s[i]<='z')
   v[s[i]-'a']++;
for(int i=0;i<26;i++)
 if(v[i])
  cout<<(char)(i+'a')<<" "<<v[i]<<endl;

Intr-un fisier sunt maxim 100000 de numere intregi din [-100,100]. Sa se afiseze sirul ordonat.
int v[201]={0};
while(fin>>x)
  v[x+100]++;
for(i=0;i<=200;i++)
  while(v[i]--)
   cout<<i-100<<" ";
------------


#include <fstream>
#include <algorithm>
#include <cstring>
using namespace std;
ifstream fin("permrep.in");
ofstream fout("permrep.out");
unsigned x[10],n,v[26],nrlitere;
char distincte[10],s[10];
void f(unsigned k)
{
  unsigned i;
  if(k==n+1)
  {
    for(i=1;i<=n;i++)
    	fout<<distincte[x[i]];
   	fout<<'\n';
  }
  else
    for(i=1;i<=nrlitere;i++)
  	{
    	if(v[distincte[i]-'a']>0)
    	{
    	    x[k]=i;
    	    v[distincte[i]-'a']--;
            f(k+1);
            v[distincte[i]-'a']++;
            x[k]=0;
    	}
  }   
}
int main()
{
  fin>>s;n=strlen(s);
  for(unsigned i=0;i<n;i++) v[s[i]-'a']++;
  for(unsigned i=0;i<26;i++)
    if(v[i]) distincte[++nrlitere]=(char)(i+'a');
  f(1);
  return 0;
}


#include <iostream>

using namespace std;
int a[1025][1025];
void f(int k, int x1, int y1, int x2, int y2) {
  if (k == 1)
    return;
  int xm, ym;
  xm = (x1 + x2) / 2;
  ym = (y1 + y2) / 2;
  int i, j;
  for (i = x1; i <= xm; i++)
    for (j = y1; j <= ym; j++)
      a[i][j] = 1;
  f(k / 2, x1, ym + 1, xm, y2);
  f(k / 2, xm + 1, y1, x2, ym);
  f(k / 2, xm + 1, ym + 1, x2, y2);
}
int main() {
  int n, p;
  cin >> n;
  p = (1 << n);
  f(p, 1, 1, p, p);
  for (int i = 1; i <= p; i++) {
    {
      for (int j = 1; j <= p; j++)
        cout << a[i][j] << " ";
      cout << endl;
    }
  }
  return 0;
}

---------------------------
https://www.geeksforgeeks.org/measure-execution-time-function-cpp/
probleme propuse pattarn1,pattern2
https://www.pbinfo.ro/?pagina=probleme-lista&disciplina=0&clasa=11&tag=27&subtag=28&dificultate=1&folosesc_consola=-1&eticheta=
------------
Fill-837-pbinfo
//BORDAREA MATRICII
int dl[]={-1,1,0, 0};
int dc[]={0, 0,1,-1};
void read{}....
void fillx(int l,int c)
{
  if(a[l][c]!=1) return;
  a[l][c]=-1;
  for(int k=0;k<=3;k++)
   fillx(l+dl[k],c+dc[k]);
 // fillx(l-1,c);fillx(l+1,c);fillx(l,c-1);fillx(l,c+1);

}
int main()
{
  read();
  for(int i=1;i<=n;i++)
   for(int j=1;j<=m;j++)
    if(a[i][j]==1)
    {nr++; fillx(i,j);}
}


FILL de numarare
int fillx(int l,int c)
{
  if(a[l][c]!=1) return 0;
  a[l][c]=-1;
  return 1+fillx(l-1,c)+fillx(l+1,c)+fillx(l,c-1)+fillx(l,c+1);
}
------------
1
22
333
4444
......
nnnnnnn
-----------PROBLEME PROPUSE- PBINFO
pattern,pattern1,pattern2 
fill, grupuri,moscraciun
-----











 

